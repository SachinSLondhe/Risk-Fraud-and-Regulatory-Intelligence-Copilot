---
created: 2026-09-10T02:36:11.945Z
session: ad5c37c4-8cb1-405b-a1ea-a8ac1c201656
working_directory: C:\Sachin\Technical\Projects\GCC Hackathon\Risk, Fraud and Regulatory Intelligence Copilot
---


## Execution Plan: RegIntel Copilot

### Environment Findings
- **Role:** ACCOUNTADMIN on trial account `DZ98495` (AWS AP-SOUTHEAST-7)
- **Warehouse:** COMPUTE_WH (XS) available
- **Cortex AI:** NOT available (trial account limitation) — no COMPLETE, Search, Analyst, or Agents
- **Streamlit:** Supported (SYSTEM$STREAMLIT_NOTEBOOK_WH exists)
- **REGINTEL database:** Does not exist yet — clean start

### Key Constraint: No Cortex AI
All AI-dependent features must use **fallback paths**:
- Policy retrieval → SQL-based lexical search (ILIKE / keyword matching over policy chunks)
- Copilot Q&A → Parameterized SQL query templates with intent matching (rule-based, not LLM)
- Draft finding generation → Template-based narrative assembly from evidence fields
- Report generation → SQL-driven template filling (no LLM summarization)

### Execution Phases (14 steps, sequential)

**Phase 1: Project structure** — Create the `regintel-copilot/` folder tree with all subdirectories and placeholder files per prompt 02.

**Phase 2: Environment check script** — `00_environment_check.sql` to verify roles, warehouses, and Cortex availability.

**Phase 3: RBAC & database foundation** — `01_roles_and_database.sql` with 6 roles, 7 schemas, least-privilege grants, and `13_cleanup.sql`.

**Phase 4: Core domain tables** — `02_core_tables.sql` with all 12 tables (RAW, RISK, CASE_MGMT, AUDIT schemas) plus curated views.

**Phase 5: Synthetic data** — `03_synthetic_data.sql` with 500 customers, 700 accounts, ~50K transactions, 10 planted suspicious patterns. Pin demo IDs (A000123, C000017). Split into: base entities → normal transactions → planted scenarios → daily balances.

**Phase 6: Data quality checks** — `04_data_quality.sql` for row counts, referential integrity, null checks.

**Phase 7: Risk features & rules** — `05_risk_features.sql` (14 feature views) + `06_risk_rules.sql` (9 versioned rules: AML_001-004, FRD_001-003, CRD_001, LIQ_001) + `V_ALERT_CANDIDATES` view.

**Phase 8: Alert pipeline** — `07_alert_pipeline.sql` to materialize alerts from candidates into RISK.ALERTS with evidence snapshots.

**Phase 9: Policy documents & search** — 4 synthetic policy markdown files + `08_policy_search.sql` with SQL-based lexical search (fallback, no Cortex Search).

**Phase 10: Semantic layer / QA contract** — `09_semantic_layer.sql` with parameterized query templates + intent routing logic (rule-based keyword matching). Structured JSON response contract.

**Phase 11: Case management & audit** — `10_case_management.sql` (workflow procedures, state transitions, evidence hashing) + `11_audit_logging.sql`.

**Phase 12: Streamlit app** — `streamlit_app.py` with 9 pages: Executive Overview, Risk Signal Monitor, Alert Investigation, Evidence Explorer, Policy Intelligence (SQL-based), Case Management, Report Builder, Audit Trail, About. Uses parameterized queries instead of Cortex for the copilot interaction.

**Phase 13: Tests & evaluation** — `test_data_quality.sql`, `test_risk_rules.sql`, `test_evidence_integrity.sql`, `test_rbac.sql` + precision/recall against planted scenarios.

**Phase 14: Docs, demo script, hardening** — `architecture.md`, `demo_script.md`, `judging_criteria.md`, `threat_model.md`, README update, final review.

### Team Decision: **Use teammates**
This project has 30+ files to create across 4 distinct domains (SQL DDL/DML, policy documents, Python/Streamlit, documentation) with clear ownership boundaries and minimal overlap. I'll use parallel teammates for independent phases.

### Verification
- Execute all SQL scripts in order and validate with data quality checks
- Run all test scripts
- Deploy Streamlit app and walk through all 8 demo questions
- Verify RBAC by testing with each role
